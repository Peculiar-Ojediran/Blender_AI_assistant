# Unit Tests

Pure Python tests for operation schemas, validation, context serialization, workflow state, provider behavior, risk rules, and history. These tests must not require Blender or live API calls.
